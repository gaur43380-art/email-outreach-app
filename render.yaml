# # render.yaml

# services:
#   - type: web
#     name: email-outreach-app
#     env: python
#     region: oregon
#     plan: free
#     buildCommand: pip install -r requirements.txt
#     startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
#     envVars:
#       - key: PYTHON_VERSION
#         value: 3.11.0
#       - key: ENV
#         value: production
#       - key: SECRET_KEY
#         generateValue: true
#       - key: WEBSITE_ACCESS_ID
#         sync: false
#       - key: WEBSITE_ACCESS_PASSWORD
#         sync: false
#       - key: MAX_EMAILS_PER_DAY
#         value: 50
#       - key: MIN_DELAY_SECONDS
#         value: 120
#       - key: MAX_DELAY_SECONDS
#         value: 300
# ```

# ---

# ## 📁 **STEP 8: Final Project Structure**
# ```
# email-outreach-app/
# ├── backend/
# │   ├── __init__.py
# │   ├── main.py
# │   ├── config.py
# │   ├── api/
# │   ├── auth/
# │   ├── db/
# │   ├── models/
# │   ├── services/
# │   ├── utils/
# │   └── workers/
# ├── frontend/
# │   ├── *.html
# │   ├── css/
# │   └── js/
# ├── credentials/
# │   ├── client_secret.json  (gitignored)
# │   └── sheets_service.json (gitignored)
# ├── .env                     (gitignored)
# ├── .gitignore
# ├── requirements.txt
# ├── README.md
# ├── render.yaml
# └── email_outreach.db        (gitignored)

services:
  - type: web
    name: email-outreach-app
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10